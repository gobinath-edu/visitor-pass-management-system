import Input from "../common/Input.jsx";
export default function ReportFilters({ period, setPeriod, range, setRange, onApply }) { return <div className="report-toolbar"><select className="input compact" value={period} onChange={e=>setPeriod(e.target.value)}><option value="TODAY">Today</option><option value="WEEK">This Week</option><option value="CUSTOM">Custom Date Range</option></select>{period==="CUSTOM"&&<><Input type="date" label="From" value={range.startDate} onChange={e=>setRange(p=>({...p,startDate:e.target.value}))}/><Input type="date" label="To" value={range.endDate} onChange={e=>setRange(p=>({...p,endDate:e.target.value}))}/><button className="btn btn-primary" type="button" onClick={onApply}>Apply</button></>}</div>; }
